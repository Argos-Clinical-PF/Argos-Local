.env
# Copias de seguridad del .env: contienen los mismos secretos que el original.
.env.backup*
.env.bak
.env.*.local
.DS_Store
terraform/.terraform/
terraform/.build/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
terraform/*.tfplan
terraform/*.pem
terraform/*.key
terraform/argos-deploy.pub

# Ajustes locales de cada desarrollador: nunca se versionan.
docker-compose.override.yml
